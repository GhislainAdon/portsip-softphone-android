<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/group_select"
        android:visible="false">
        <item
            android:id="@+id/menu_contact_select"
            app:showAsAction="always|withText"
            android:title="@string/select_all">
        </item>
    </group>

    <group android:id="@+id/group_normal">
        <item android:id="@+id/menu_contact_search"
            android:title="@android:string/search_go"
            android:icon="@drawable/nav_srech_ico_def"
            app:showAsAction="collapseActionView|ifRoom"
            app:actionViewClass="androidx.appcompat.widget.SearchView" />
        <item
            android:id="@+id/menu_contact_add"
            app:showAsAction="always|withText"
            android:icon="@drawable/nav_add_man_ico_def"
            android:title="@string/history_fragment_detail_newcontact">
        </item>
    </group>
</menu>
