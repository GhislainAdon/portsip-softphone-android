<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_diapad_select_contact"
        app:showAsAction="always|withText"
        android:orderInCategory="300"
        android:title="0.0$"
        android:visible="false"/>

    <item
        android:id="@+id/status_no_disturb"
        app:showAsAction="always|withText"
        android:orderInCategory="300"
        android:enabled="false"
        android:icon="@drawable/dial_nav_nodisturbe_ico"
        android:visible="false"/>

    <item
        android:id="@+id/status_forward"
        app:showAsAction="always|withText"
        android:enabled="false"
        android:orderInCategory="300"
        android:icon="@drawable/dial_nav_forward_ico"
        android:visible="false"/>
        <!--android:showAsAction="always|withText"非appcompact主题下，要用这个，否则会导致无法显示-->
        <!--app:showAsAction="always|withText"appcompact主题下，要用这个，否则会导致无法显示-->
</menu>
